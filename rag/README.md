# RAG Workspace

Runtime implementation lives in `backend/app/agents/knowledge_store.py`, `embedding_service.py`, and the admin knowledge endpoints.

This directory is reserved for repository-level RAG assets such as:

- ingestion specifications
- retrieval evaluation reports
- benchmark prompts
- data quality checklists

At present, operational documentation is captured in `docs/RAG.md`.
