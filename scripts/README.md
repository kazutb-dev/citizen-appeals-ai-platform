# Repository Scripts

This directory is reserved for top-level operational scripts that do not belong exclusively to backend or frontend application code.

Examples of appropriate future contents:

- release helpers
- repository bootstrap tasks
- label and milestone sync tooling
- local environment validation scripts

Backend-specific scripts currently live in `backend/app/scripts/`.
