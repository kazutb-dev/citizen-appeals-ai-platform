# Tests

This directory is reserved for repository-level automated tests and scenario validation assets.

Current backend and frontend validation is primarily executed through:

- backend compile and migration checks
- frontend lint and production build
- Docker Compose runtime verification

As integration coverage expands, end-to-end, workflow, and regression suites should be added here.
